{
  "name": "@lmc-eu/spirit-icons",
  "version": "0.2.0",
  "description": "Icons for Spirit Design System",
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "directory": "dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lmc-eu/spirit-design-system.git",
    "directory": "packages/icons"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/svg && cp -r package.json README.md dist/ && node ./scripts/build.js"
  }
}
