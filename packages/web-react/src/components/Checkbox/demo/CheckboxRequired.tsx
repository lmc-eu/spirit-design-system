import React from 'react';
import Checkbox from '../Checkbox';

const CheckboxRequired = () => (
  <Checkbox id="checkbox-required" name="checkboxRequired" label="Checkbox Label" isRequired onChange={() => {}} />
);

export default CheckboxRequired;
