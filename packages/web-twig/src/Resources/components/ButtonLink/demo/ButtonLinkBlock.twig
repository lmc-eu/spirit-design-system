<Grid cols="{{ { mobile: 1, desktop: 3 } }}" spacingY="space-1100">
    {% for size in sizes %}

        {% set title = 'Size ' ~ size %}

        <DocsSection container="none" hasPadding={ false } title={{ title }}>
            {% for color in colors %}
                <Grid cols="1" spacing="space-400">
                    <ButtonLink href="#" color={ color } size={ size } onclick="alert('Button clicked')">
                        Button {{ color }}
                    </ButtonLink>
                    <ButtonLink href="#" color={ color } size={ size }>
                        <Icon name="chevron-left" marginRight="space-400" />
                        Return back
                    </ButtonLink>
                </Grid>
            {% endfor %}
        </DocsSection>

    {% endfor %}
</Grid>
