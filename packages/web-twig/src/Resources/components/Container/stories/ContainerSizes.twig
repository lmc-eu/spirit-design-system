{% for size in sizes %}
    {% set title = 'Size ' ~ size %}
    <DocsSection title={{ title }} stackAlignment="stretch" container="heading-only">
        <Container size={ size }>
            <DocsBox>Container--{{ title }}</DocsBox>
        </Container>
    </DocsSection>
{% endfor %}
