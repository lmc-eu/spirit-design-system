<Spinner />

<!-- Render as primary -->
<Spinner color="primary" />

<!-- Render as secondary -->
<Spinner color="secondary" />

<!-- Render visible to assistive technologies -->
<Spinner ariaHidden={ false } />

<!-- Render with all props -->
<Spinner
    color="tertiary"
    UNSAFE_className="my-custom-class"
/>
