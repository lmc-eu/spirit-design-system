{% set _className = _spiritClassPrefix ~ 'Header__nav' %}
{% set _class = (props.class is defined) ? ' ' ~  props.class : '' %}

<ul class="{{ _className }}{{ _class }}">
{% block content %}{% endblock %}
</ul>
