{% set _rootClassName = _spiritClassPrefix ~ 'Header__nav' %}
{% set _class = (props.class is defined) ? props.class : '' %}

{# Miscellaneous #}
{%- set _classNames = [ _rootClassName, _class ] -%}

<ul {{ classProp(_classNames) }}>
{% block content %}{% endblock %}
</ul>
