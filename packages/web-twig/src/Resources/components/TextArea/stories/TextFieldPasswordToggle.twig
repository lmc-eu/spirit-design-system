<TextArea
    id="textareaPasswordToggle"
    label="Password Toggle"
    hasPasswordToggle
    name="textareaPasswordToggle"
    placeholder="Password must be at least 6 characters long"
/>

<TextArea
    id="textareaPasswordToggle"
    label="Password Toggle"
    hasPasswordToggle
    isDisabled
    name="textareaPasswordToggle"
    placeholder="Password must be at least 6 characters long"
/>
