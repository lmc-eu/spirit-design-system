{# Use the link styling also for `button` elements: #}
{%- set newProps = props | merge({ rootClass: 'HeaderDialogLink' }) -%}
{%- set renderedContent -%}
    {% block content %}{% endblock %}
{%- endset -%}

{%- embed '@spirit/Header/_abstracts/Button.twig' with { props: newProps, renderedContent } only -%}
    {%- block content -%}
        {{- renderedContent -}}
    {%- endblock -%}
{%- endembed -%}
