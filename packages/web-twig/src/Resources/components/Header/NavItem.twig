{% set _rootClassName = _spiritClassPrefix ~ 'Header__navItem' %}
{% set _class = (props.class is defined) ? ' ' ~  props.class : '' %}

{# Miscellaneous #}
{%- set _classNames = [ _rootClassName, _class ] -%}

<li {{ classProp(_classNames) }}>
{% block content %}{% endblock %}
</li>
