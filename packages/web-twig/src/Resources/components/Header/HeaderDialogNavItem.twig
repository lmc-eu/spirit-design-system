{%- set newProps = props | merge({ rootClass: 'HeaderDialogNavItem' }) -%}
{%- set renderedContent -%}
    {% block content %}{% endblock %}
{%- endset -%}

{%- embed '@spirit/Header/_abstracts/NavItem.twig' with { props: newProps, renderedContent } only -%}
    {%- block content -%}
        {{- renderedContent -}}
    {%- endblock -%}
{%- endembed -%}
