<Modal id="example-image-preview" titleId="example-image-preview-title">
    <ModalDialog>
        <ModalHeader modalId="example-image-preview" titleId="example-image-preview-title">
            Image Preview
        </ModalHeader>
        <ModalBody>
            <div class="pt-400 pt-tablet-600" data-example-content></div>
        </ModalBody>
        <ModalFooter>
            <Button
                color="primary"
                data-spirit-target="#example-image-preview"
                data-spirit-dismiss="modal"
            >
                Confirm
            </Button>
            <Button
                color="secondary"
                data-spirit-target="#example-image-preview"
                data-element="cancel"
            >
                Cancel
            </Button>
        </ModalFooter>
    </ModalDialog>
</Modal>

<FileUploader data-spirit-toggle="fileUploader" id="example-custom-image-preview">
    <template data-spirit-snippet="item">
        <FileUploaderAttachment onEdit="editHandler()" generateImagePreview />
    </template>
    <FileUploaderInput
        helperText="Max size of each file is 10 MB"
        id="file-uploader-with-image-preview"
        label="Label"
        multiple
        name="fileUploaderWithImagePreview"
        pickAFileText="Upload your file(s)"
    />
    <FileUploaderList headingId="attachments-custom-image-preview" />
</FileUploader>

{{ encore_entry_script_tags('fileUploaderImagePreview') }}
