<section class="docs-Section">

    <h2 class="docs-Heading">Dragging not Available</h2>

    <p>
        ℹ️ Visual example only, no attachments will show up in this demo (because the JavaScript handler is not being initialized).
    </p>

    <FileUploader>
        <FileUploaderInput
            helperText="Max file size is 10 MB"
            id="example-dragging-not-available"
            label="Label"
            name="example-dragging-not-available"
        />
    </FileUploader>

</section>
