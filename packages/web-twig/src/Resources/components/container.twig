{% set _className = _spiritClassPrefix ~ 'Container' %}
{% set _class = (props.class is defined) ? ' ' ~  props.class : '' -%}

<div class="{{ _className }}{{ _class }}">
{%- block content %}{% endblock %}
</div>
