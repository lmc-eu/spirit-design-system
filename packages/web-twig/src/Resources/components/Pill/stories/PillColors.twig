{% set colors = [
    'primary', 'secondary', 'tertiary', 'inverted', 'selected', 'unselected', 'success', 'informative', 'warning', 'danger'
] %}

{% for color in colors %}
    <Pill color={ color }>3</Pill>
{% endfor %}
