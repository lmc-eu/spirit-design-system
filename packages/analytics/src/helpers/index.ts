import { errorMessage, infoMessage } from './message';
import { getModuleName } from './moduleName';
import { __dirname, __filename, getOutputPath } from './path';
import { getVersions } from './versions';
import { timestamp } from './date';

export { getModuleName, infoMessage, errorMessage, __dirname, __filename, getOutputPath, getVersions, timestamp };
