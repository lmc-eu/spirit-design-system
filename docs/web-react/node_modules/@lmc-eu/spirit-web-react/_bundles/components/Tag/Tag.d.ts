import { WithChildren } from '../../types/main';
declare type Color = 'default' | 'informative' | 'success' | 'warning' | 'danger';
declare type Theme = 'light' | 'dark';
export interface TagProps extends WithChildren {
    /**
     * Emotion color variant.
     */
    color: Color;
    /**
     * Theme
     */
    theme: Theme;
}
export declare const Tag: {
    ({ color, theme, children }: TagProps): JSX.Element;
    defaultProps: {
        color: string;
        theme: string;
    };
};
export default Tag;
