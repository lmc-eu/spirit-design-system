{
  "name": "spirit",
  "version": "1.0.0",
  "description": "Spirit Design System",
  "keywords": [
    "spirit",
    "design",
    "system"
  ],
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=14",
    "yarn": "~1.22.10"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "examples": "yarn examples:build",
    "examples:install": "cd examples/web && yarn install && cd ../web-react && yarn install",
    "examples:build": "cd examples/web && yarn build && cd ../web-react && yarn build",
    "diff": "lerna diff",
    "changed": "lerna changed",
    "list": "lerna ls",
    "commit:lint:test": "yarn commitlint --from HEAD~1 --to HEAD --verbose",
    "format": "yarn format:check",
    "format:check": "prettier --list-different 'packages/**/*.{js,jsx,ts,tsx,scss}'",
    "format:fix": "prettier --write 'packages/**/*.{js,jsx,ts,tsx,scss}'",
    "prepare": "husky install",
    "storybook": "start-storybook -p 6006 --no-manager-cache",
    "storybook:build": "build-storybook -o ./.storybook/build",
    "test": "lerna run test",
    "types": "lerna run types",
    "release": "yarn build && lerna publish"
  },
  "devDependencies": {
    "@babel/core": "^7.13.16",
    "@babel/preset-react": "^7.14.5",
    "@commitlint/cli": "^13.1.0",
    "@lmc-eu/commitlint-config": "^1.0.0",
    "@lmc-eu/prettier-config": "^1.0.1",
    "@storybook/addon-actions": "^6.2.9",
    "@storybook/addon-essentials": "^6.2.9",
    "@storybook/addon-links": "^6.2.9",
    "@storybook/addons": "^6.2.9",
    "@storybook/html": "^6.2.9",
    "@storybook/react": "^6.3.8",
    "@storybook/theming": "^6.2.9",
    "babel-loader": "^8.2.2",
    "css-loader": "^5.0.0",
    "husky": "^7.0.2",
    "lerna": "^4.0.0",
    "prettier": "^2.2.1",
    "webpack": "^5.51.1",
    "webpack-cli": "^4.7.2",
    "sass-loader": "^10.0.0",
    "style-loader": "^2.0.0"
  }
}
